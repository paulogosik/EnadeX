# Desenvolvimento e verificação do enade_time (não são necessários para rodar
# a API nem os scripts de benchmark).
pytest>=8.0
httpx>=0.27          # TestClient do FastAPI
python-docx>=1.1     # gerador do documento acadêmico (docs/geradores)
python-pptx>=1.0     # gerador da apresentação
